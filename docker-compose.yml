services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: sqlite:///./data/education.db
      SECRET_KEY: change-me-in-production-secret-key-123
    volumes:
      # Host uploads so course videos (e.g. course1/intro.mp4) are visible when using Docker
      - ./backend/uploads:/app/uploads
      - backend_db:/app/data

  frontend:
    build:
      context: ./frontend-next
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      BACKEND_URL: http://backend:8000
    depends_on:
      - backend

volumes:
  backend_db:
